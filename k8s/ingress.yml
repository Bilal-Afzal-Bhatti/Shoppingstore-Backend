apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ecommerce-backend-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:us-east-1:27047:certificate/e5caaf3d-8a16-4128-a8
    alb.ingress.kubernetes.io/ssl-redirect: '443'
    
    # 🔗 Group name merges separate ingress resources into ONE AWS ALB
    alb.ingress.kubernetes.io/group.name: "ecommerce-alb-group"
    alb.ingress.kubernetes.io/group.order: '1' # 👈 Higher priority for /api routes
spec:
  ingressClassName: alb
  rules:
    - host: nexcuscart.com
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: ecommerce-backend-service
                port:
                  number: 5000